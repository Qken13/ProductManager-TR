<UserControl x:Class="ProductManager.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>

        <!-- BACKGROUND -->
        <Image Source="Assets/tech.png"
               Opacity="0.05"
               Stretch="UniformToFill"/>

        <Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TitleText"
                           Text="Ürün Yönetici"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="10"/>

                <!-- USD RATE -->
                <Border Grid.Column="1"
                        Background="White"
                        CornerRadius="12"
                        Padding="10"
                        Margin="10">

                    <StackPanel>

                        <TextBlock Text="Dolar fiyatı 💵"
               FontSize="14"
               FontWeight="SemiBold"
               Foreground="Black"/>

                        <TextBlock x:Name="TxtDollarRate"
               Text="..."
               FontSize="20"
               FontWeight="Bold"
               Foreground="#16A34A"/>

                        <TextBlock x:Name="TxtLastUpdate"
               Text="son güncelleme: --:--:--"
               FontSize="11"
               Foreground="Gray"
               Margin="0,3,0,0"/>

                    </StackPanel>

                </Border>

            </Grid>

            <!-- WELCOME -->
            <TextBlock Grid.Row="1"
                       Text="Ürün Yöneticisine Hoşgeldiniz!"
                       FontSize="16"
                       Foreground="Gray"
                       Margin="10,0,10,20"/>

            <!-- CARDS -->
            <UniformGrid Grid.Row="3"
                         Columns="3">

                <!-- PRODUCTS -->
                <Border CornerRadius="12"
                        Background="White"
                        Margin="8"
                        Padding="15">

                    <StackPanel>

                        <TextBlock Text="Ürün Sayısı📦"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="Black"/>

                        <TextBlock x:Name="TxtProducts"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Foreground="Black"
                                   Text="0"/>

                    </StackPanel>

                </Border>

                <!-- SALES -->
                <Border CornerRadius="12"
                        Background="White"
                        Margin="8"
                        Padding="15">

                    <StackPanel>

                        <TextBlock Text="Satışlar💰"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="Black"/>

                        <TextBlock x:Name="TxtSales"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Foreground="Black"
                                   Text="0 ₺"/>

                    </StackPanel>

                </Border>

                <!-- STOCK VALUE USD -->
                <Border CornerRadius="12"
                        Background="White"
                        Margin="8"
                        Padding="15">

                    <StackPanel>

                        <TextBlock Text="Stok değeri 🏪"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="Black"/>

                        <TextBlock x:Name="TxtStockValue"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Foreground="#2563EB"
                                   Text="0 $"/>

                    </StackPanel>

                </Border>

            </UniformGrid>

        </Grid>

    </Grid>
</UserControl>